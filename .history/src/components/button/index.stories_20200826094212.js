import React from "react";
import Button from "./index";

export default { title: "Components|Filter", component: Button };

export const Default = () => {
    return <Button name="Normal"></Button>;
};
